# If unset, the portal will default to http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001

# Required only if you want embedded Stripe Checkout (keeps users in the portal).
# Use your Stripe *publishable* key (pk_test_...)
VITE_STRIPE_PUBLISHABLE_KEY=
