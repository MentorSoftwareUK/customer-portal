Events + Meetings Only (Go-live) — Monday.com tasks (plain English, one per line)

Confirm what we’re launching (ONLY Events + Meetings; everything else hidden/disabled) — 0.5–1h
Confirm how Meetings will work on day 1 (links to Training + Customer Success calendars) — 0.5–1h
Confirm Events payments on day 1 (free-only vs paid tickets) — 0.5h

Set the “on/off switches” in Admin Settings so only Events + Meetings are ON — 1–2h
Remove/hide menu items for anything not in scope (Tickets, Invoices, Knowledge Base, Videos, Documents, etc.) — 2–4h
Make sure users can’t access out-of-scope pages even with a direct link — 2–4h
Make the system safe if settings can’t load (default to keeping out-of-scope areas OFF) — 1–2h

Event emails: update confirmation + reminder emails to include the event joining link and key details — 2–4h
Test Events end-to-end (log in → register → receive email → view event) — 0.5–1 day

Decide how Events will be added/updated for launch (admin enters them vs we load them into the system directly) — 0.5–1h
If “load them in directly”: prepare a simple runbook + add the initial launch events — 2–4h
If “admin enters them”: add an admin screen to create/edit events — 1–2 days

Meetings: replace placeholder meetings with real calendar links for Training + Customer Success — 0.5–1 day
Test Meetings page (links work, correct wording, nothing confusing) — 1–2h

Set up the live environment (hosting + live database + security/backup basics) — 1–2 days
Set up email sending for live (email provider settings + test deliverability) — 0.5–1 day
Publish the Portal and the API to the live environment and connect them together — 0.5–1 day
Set up a staging/pre-live environment for testing (recommended) — 0.5–1 day

User Acceptance Testing (UAT): run a simple checklist with the business (login, events, emails, meetings, “everything else is off”) — 0.5–1 day
Go-live day: final checks + switch live + announce to users — 2–4h
Post go-live support buffer (monitoring + quick fixes) — 0.5–1 day
